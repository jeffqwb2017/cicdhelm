apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-chart-cicd.fullname" . }}-test-connection"
  labels:
{{ include "helm-chart-cicd.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "helm-chart-cicd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
